from setuptools import setup, find_packages

setup(
    name='botteddotgay',
    version='0.1',
    description='Python bindings for the botted.gay API',
    author='sang',
    packages=find_packages(),
)
